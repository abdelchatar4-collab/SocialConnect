# Configuration de l'environnement (EXEMPLE)
# COPIER CE FICHIER EN 'docker.env' ET REMPLIR LES VALEURS

# -----------------------------------------------------------------------------
# 1. APPLICATION
# -----------------------------------------------------------------------------
NODE_ENV=production
NEXT_PUBLIC_FORCE_LOCALSTORAGE=false

# URL de l'application (ex: https://votre-domaine.com)
NEXTAUTH_URL=http://localhost:3000

# Secret pour l'authentification (Générer avec: openssl rand -base64 32)
NEXTAUTH_SECRET=changeme_please_generated_secure_secret_here

# -----------------------------------------------------------------------------
# 2. BASE DE DONNÉES (MariaDB/MySQL)
# -----------------------------------------------------------------------------
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=mysql://pasqweb:changeme_strong_password@mariadb:3306/gestion_usagers_db

# -----------------------------------------------------------------------------
# 3. AUTHENTIFICATION GOOGLE (OAUTH2)
# -----------------------------------------------------------------------------
# À récupérer sur Google Cloud Console
GOOGLE_CLIENT_ID=votre-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=votre-client-secret

# -----------------------------------------------------------------------------
# 4. SÉCURITÉ CLOUDFLARE (Optionnel)
# -----------------------------------------------------------------------------
ENABLE_CLOUDFLARE_DEV=false
CLOUDFLARE_ACCESS_DOMAIN=votre-domaine.cloudflareaccess.com
CLOUDFLARE_ACCESS_AUD=votre-audience-token
